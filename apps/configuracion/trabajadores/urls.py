from django.conf.urls import patterns, include, url
# from .views import RegistrarMotivos, ListarMotivos, EditarMotivos, EliminarMotivos

urlpatterns = patterns('',
    # url(r'registro/$', RegistrarMotivos.as_view(),name='registro_motivo'),
    # url(r'listar/$', ListarMotivos.as_view(),name='listar_motivo'),
    # url(r'editar/(?P<pk>\d+)$', EditarMotivos.as_view(),name='editar_motivo'),
    # url(r'eliminar/(?P<pk>\d+)$', EliminarMotivos.as_view(),name='eliminar_motivo'),
    )